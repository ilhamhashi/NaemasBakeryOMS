<UserControl x:Class="OrderManagerDesktopUI.Views.DashBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderManagerDesktopUI.Views" xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80*" />
            <RowDefinition Height="20*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="65*"/>
                <ColumnDefinition Width="35*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource whiteBorder}" Margin="15 0 7 7">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="8*"/>
                        <RowDefinition Height="92*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Margin="20 10 0 0"
                               Text="Upcoming Orders" 
                               FontSize="20"
                               HorizontalAlignment="Left" 
                               VerticalAlignment="Center"/>

                    <DataGrid Grid.Row="1" RowStyle="{DynamicResource DataGridRowStyle1}" 
                          ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                          CellStyle="{DynamicResource DataGridCellStyle1}" 
                          Style="{DynamicResource DataGridStyle1}"
                          ItemsSource="{Binding UpcomingOrders, UpdateSourceTrigger=PropertyChanged}"
                          VerticalAlignment="Stretch">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Order" IsReadOnly="True" CanUserResize="False" Width="auto" 
                        Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Date" IsReadOnly="True" Width="auto"
                        Binding="{Binding Date, StringFormat={}{0:d}, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Customer" IsReadOnly="True" Width="auto">
                                <DataGridTextColumn.Binding>
                                    <MultiBinding StringFormat="{}{0} {1}">
                                        <Binding Path="Customer.FirstName" />
                                        <Binding Path="Customer.LastName" />
                                    </MultiBinding>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="PickUp" IsReadOnly="True" Width="auto"
                        Binding="{Binding PickUp.Date, StringFormat={}{0:g}, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Status" IsReadOnly="True" Width="auto"
                        Binding="{Binding Status, UpdateSourceTrigger=PropertyChanged}"/>

                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
            
            <Border Grid.Column="1" Style="{StaticResource whiteBorder}" Margin="7 0 0 7">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="8*"/>
                        <RowDefinition Height="92*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Margin="20 10 0 0"
                               Text="Pending Payment" 
                               FontSize="20"
                               HorizontalAlignment="Left" 
                               VerticalAlignment="Center"/>

                    <DataGrid Grid.Row="1" RowStyle="{DynamicResource DataGridRowStyle1}" 
                              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                              CellStyle="{DynamicResource DataGridCellStyle1}" 
                              Style="{DynamicResource DataGridStyle1}"
                              ItemsSource="{Binding PendingPaymentOrders, UpdateSourceTrigger=PropertyChanged}"
                              VerticalAlignment="Stretch">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Order" IsReadOnly="True" CanUserResize="False" Width="auto" 
                                                Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Customer" IsReadOnly="True" Width="*"
                                                Binding="{Binding Customer.FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Status" IsReadOnly="True" Width="*"
                                                Binding="{Binding Status, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" 
                    Style="{StaticResource dashboardStats}"
                    Margin="15 7 7 0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="revenue" TextWrapping="Wrap" 
                               FontSize="12" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding WeeklyRevenue, StringFormat={}{0:C0}, UpdateSourceTrigger=PropertyChanged}" 
                               FontSize="25" TextAlignment="Center" VerticalAlignment="Center" Margin="7"/>
                    <TextBlock Text="this week" TextWrapping="Wrap" 
                               FontSize="20" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" 
                    Style="{StaticResource dashboardStats}" 
                    Margin="7 7 7 0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="revenue" TextWrapping="Wrap" 
                               FontSize="12" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding MonthlyRevenue, StringFormat={}{0:C0}, UpdateSourceTrigger=PropertyChanged}" 
                               FontSize="25" TextAlignment="Center" VerticalAlignment="Center" Margin="7"/>
                    <TextBlock Text="this month" TextWrapping="Wrap" 
                               FontSize="20" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" 
                    Style="{StaticResource dashboardStats}" 
                    Margin="7 7 7 0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="product" TextWrapping="Wrap" 
                               FontSize="12" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding MostPopularProduct, UpdateSourceTrigger=PropertyChanged}" 
                               FontSize="25" TextAlignment="Center" VerticalAlignment="Center" Margin="7"/>
                    <TextBlock Text="most popular" TextWrapping="Wrap" 
                               FontSize="20" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>            </Border>
            <Border Grid.Column="3" 
                    Style="{StaticResource dashboardStats}" 
                    Margin="7 7 0 0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="orders" TextWrapping="Wrap" 
                               FontSize="12" TextAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding MonthlyOrdersCount, UpdateSourceTrigger=PropertyChanged}" 
                               FontSize="25" TextAlignment="Center" VerticalAlignment="Center" Margin="7"/>
                    <TextBlock Text="this month" TextWrapping="Wrap" 
                               FontSize="20" TextAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
