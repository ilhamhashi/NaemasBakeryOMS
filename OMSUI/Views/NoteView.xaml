<UserControl x:Class="OrderManagerDesktopUI.Views.NoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderManagerDesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:NewOrderView}}">
        <Grid Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  >
            <TextBlock Margin="5,2" MinWidth="50" FontSize="14" Text="Write a note here..." 
                       Visibility="{Binding ElementName=txtNoteBox, Path=Text.IsEmpty, Converter={StaticResource MyBoolToVisibilityConverter}}" 
                       IsHitTestVisible="False" />
            <TextBox Text="{Binding DataContext.NoteContent, UpdateSourceTrigger=PropertyChanged}" 
                     BorderThickness="0" FontSize="14"
                     TextWrapping="Wrap" AcceptsReturn="True" MinWidth="50" Name="txtNoteBox" Background="Transparent" />
        </Grid>

    </Grid>
</UserControl>
