<UserControl x:Class="OrderManagerDesktopUI.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderManagerDesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource whiteBorder}" 
                Margin="15 0 0 7">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="80*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Margin="20 10 0 0"
                           Text="Orders" 
                           FontSize="20"
                           HorizontalAlignment="Left" 
                           VerticalAlignment="Center"/>
                
                <StackPanel Grid.Row="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0 0 15 0">
                    <TextBlock Text="Search order (id, orderdate, customer, pickup, status)" 
                               FontSize="11"
                               Margin="10"/>
                    <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center"
                             Width="200"
                             Height="30"/>
                </StackPanel>

                <!-- list of orders -->
                <DataGrid Grid.Row="2" RowStyle="{DynamicResource DataGridRowStyle1}" 
                              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                              CellStyle="{DynamicResource DataGridCellStyle1}" 
                              Style="{DynamicResource DataGridStyle1}"
                              ItemsSource="{Binding OrdersCollectionView, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedOrder, UpdateSourceTrigger=PropertyChanged}"
                              VerticalAlignment="Stretch">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Order" IsReadOnly="True" CanUserResize="False" Width="auto" 
                                                Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="Date" IsReadOnly="True" Width="*"
                                                Binding="{Binding Date, StringFormat={}{0:d}, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="Customer" IsReadOnly="True" Width="*">
                            <DataGridTextColumn.Binding>
                                <MultiBinding StringFormat="{}{0} {1}">
                                    <Binding Path="Customer.FirstName" />
                                    <Binding Path="Customer.LastName" />
                                </MultiBinding>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="PickUp" IsReadOnly="True" Width="*"
                                                Binding="{Binding PickUp.Date, StringFormat={}{0:d}, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="Status" IsReadOnly="True" Width="*"
                                                Binding="{Binding Status, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="Note" IsReadOnly="True" Width="*"
                                            Binding="{Binding Note.Content, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="1" 
                Style="{StaticResource whiteBorder}" 
                Margin="15 7 0 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="15*"/>
                    <RowDefinition Height="85*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="15 5 0 0">
                    <TextBlock Text="Details - Order #" 
                               FontSize="18"/>
                    <TextBlock Text="{Binding SelectedOrder.Id, UpdateSourceTrigger=PropertyChanged}" 
                               FontSize="18"/>
                </StackPanel>

                <!-- Orderlines from the selected order -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="auto">
                    <DataGrid RowStyle="{DynamicResource DataGridRowStyle1}" 
                              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                              CellStyle="{DynamicResource DataGridCellStyle1}" 
                              Style="{DynamicResource DataGridStyle1}"
                              ItemsSource="{Binding SelectedOrder.OrderLines, UpdateSourceTrigger=PropertyChanged}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" IsReadOnly="True" CanUserResize="False" Width="auto" 
                                                Binding="{Binding LineNumber, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Product" IsReadOnly="True" CanUserResize="False" Width="*" 
                                                Binding="{Binding Product.Name, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Size" IsReadOnly="True" Width="*"
                                                Binding="{Binding Size, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Taste" IsReadOnly="True" Width="*"
                                                Binding="{Binding Taste, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Quantity" IsReadOnly="True" CanUserResize="False" Width="*" 
                                                Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Price" IsReadOnly="True" Width="*"
                                                Binding="{Binding Price, StringFormat={}{0:C2}, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Discount" IsReadOnly="True" Width="*"
                                                Binding="{Binding Discount, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
