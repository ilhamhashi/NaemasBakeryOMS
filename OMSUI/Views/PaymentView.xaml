<UserControl x:Class="OrderManagerDesktopUI.Views.PaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderManagerDesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DataGrid ItemsSource="{Binding Payments, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding SelectedPayment, UpdateSourceTrigger=PropertyChanged}"
                  RowStyle="{DynamicResource DataGridRowStyle1}" 
              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
              CellStyle="{DynamicResource DataGridCellStyle1}" 
              Style="{DynamicResource DataGridStyle1}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Paymentmethod" Binding="{Binding PaymentMethod, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, UpdateSourceTrigger=PropertyChanged}"/>

            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1">
            <TextBlock Text="Select a payment method" />
            <ComboBox ItemsSource="{Binding PaymentMethods, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding SelectedPaymentMethod, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Enter amount" />
            <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Save payment" Style="{StaticResource addButton}"
                    Command="{Binding AddPaymentToOrderCommand}"/>

        </StackPanel>

    </Grid>
</UserControl>
