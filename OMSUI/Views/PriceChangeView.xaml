<UserControl x:Class="OrderManagerDesktopUI.Views.PriceChangeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderManagerDesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:NewOrderView}}" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70*"/>
            <RowDefinition Height="30*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock Text="{Binding DataContext.SelectedOrderLine.LineNumber, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding DataContext.SelectedOrderLine.Product.Name, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding DataContext.SelectedOrderLine.Price, StringFormat={}{0:C0}, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        
        <StackPanel Grid.Column="1" >
            <TextBlock Text="Reduce price" />
            <TextBox Text="{Binding DataContext.SelectedOrderLine.Discount, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="OR" HorizontalAlignment="Center"/>
            <TextBlock Text="Increase price" />
            <TextBox Text="{Binding DataContext.PriceIncreaseAmount, UpdateSourceTrigger=PropertyChanged}"/>        
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="1">


        </StackPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button
                HorizontalAlignment="Center"
                Content="Reduce Price" 
                Style="{StaticResource addButton}"
                Command="{Binding DataContext.AddDiscountCommand}"
                CommandParameter="{Binding}"/>
            <Button
                HorizontalAlignment="Center"
                Content="Increase Price" 
                Style="{StaticResource addButton}"
                Command="{Binding DataContext.AddPriceIncreaseCommand}"
                CommandParameter="{Binding}"/>
        </StackPanel>
    </Grid>
</UserControl>
